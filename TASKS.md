# Task List

- [x] 001: Project setup | Initialize Next.js 14 app with TypeScript, Tailwind CSS, ESLint, and PWA manifest in `next.config.ts`, `tailwind.config.ts`, `app/layout.tsx`, `app/page.tsx`, and `public/manifest.json`.
- [x] 002: PWA service worker | Add next-pwa config, offline fallback page `app/offline/page.tsx`, meta tags, and icons in `public/` so the app is installable on mobile home screens.
- [x] 003: App shell and navigation | Build bottom tab navigation component `components/BottomNav.tsx`, app layout wrapper `components/AppShell.tsx`, and route pages: `app/breathe/page.tsx`, `app/cold/page.tsx`, `app/progress/page.tsx`, `app/settings/page.tsx`.
- [x] 004: Design tokens and shared UI | Create `lib/constants.ts` for colors/sizes, reusable `components/ui/Button.tsx`, `components/ui/Card.tsx`, `components/ui/Modal.tsx`, and global Tailwind theme extensions.
- [x] 005: Local storage service | Build `lib/storage.ts` with typed helpers to read/write breathing sessions, cold sessions, and user preferences to localStorage with JSON serialization.
- [x] 006: Safety onboarding screen | Create `components/SafetyOnboarding.tsx` as a full-screen modal shown on first launch, listing safety rules with an acknowledge button that sets a flag in localStorage.
- [x] 007: In-session safety reminder | Build `components/SafetyReminder.tsx` dismiss dialog shown before every breathing session starts, with a "I understand" button to proceed.
- [x] 008: Breathing session config screen | Build `app/breathe/page.tsx` with round count selector (1–5), breath count selector (20/30/40), pace selector (Slow/Medium/Fast), and a "Start Session" button storing config in React state.
- [x] 009: Breathing animation component | Create `components/breathing/BreathingCircle.tsx` — an expanding/contracting circle animation driven by a pace prop (1.5s/2s/2.5s cycle) using CSS transitions or framer-motion.
- [x] 010: Power breaths phase | Build `components/breathing/PowerBreaths.tsx` that counts 20/30/40 breaths at the configured pace, displays current breath number prominently, drives the breathing animation, and auto-advances to retention phase.
- [x] 011: Retention hold phase | Build `components/breathing/RetentionHold.tsx` showing an elapsed stopwatch timer (not countdown), a tap-to-end button, and recording the retention duration when the user taps.
- [x] 012: Recovery breath phase | Build `components/breathing/RecoveryBreath.tsx` with a fixed 15-second countdown, distinct color scheme, and auto-advance to the next round or session complete.
- [x] 013: Breathing session orchestrator | Create `components/breathing/SessionRunner.tsx` that sequences PowerBreaths → RetentionHold → RecoveryBreath for N rounds, tracks per-round retention times, and transitions to session complete.
- [x] 014: Session completion screen | Build `components/breathing/SessionComplete.tsx` showing total duration, retention times per round, personal best highlight, and a 1–5 feeling scale with optional text note, then save to storage.
- [x] 015: Audio engine for tones | Build `lib/audio.ts` using Web Audio API to generate inhale tone, exhale tone, hold start chime, hold end chime, and countdown beeps, with a mute toggle stored in preferences.
- [x] 016: Wire audio to breathing phases | Integrate `lib/audio.ts` into PowerBreaths, RetentionHold, and RecoveryBreath components so tones play in sync with each phase transition and breath cycle.
- [x] 017: Cold shower timer | Build `app/cold/page.tsx` with a configurable target duration (30s–3min), start/stop controls, elapsed time display, circular progress bar, and alert vibration when target is reached.
- [x] 018: Cold session logging | After cold timer ends, show a log form with duration (auto-filled), temperature (optional), type (shower/bath/outdoor), and 1–5 rating, then save to localStorage via `lib/storage.ts`.
- [x] 019: Session history list | Build `app/progress/page.tsx` with two tabs (Breathing / Cold) listing past sessions from localStorage, showing date, duration, retention times or cold duration, sorted newest-first.
- [x] 020: Breathing retention chart | Add a Recharts line chart to the Breathing tab in `app/progress/page.tsx` plotting retention time over sessions, with average line and personal record marker.
- [x] 021: Cold exposure stats | Add cold exposure stats to the Cold tab: total minutes, current streak counter, and a simple heatmap-style calendar showing days with cold sessions using Recharts or CSS grid.
- [x] 022: Settings page | Build `app/settings/page.tsx` with audio mode toggle (tones/silent), default breathing config, cold target default, clear data button with confirmation, and app version display.
- [x] 023: Session presets | Add preset cards to the breathing config screen: Beginner (3 rounds, 30 breaths, slow), Standard (3/30/medium), Deep Practice (4/40/medium), Morning Activation (3/30/fast) that auto-fill settings.
- [x] 024: Responsive polish and dark mode | Add dark mode support via Tailwind `dark:` classes and `prefers-color-scheme`, ensure all screens are responsive on mobile viewports (375px–428px), and add smooth page transitions.
- [x] 025: Final QA and build verification | Fix any TypeScript errors, ensure `npm run build` passes with zero warnings, test full breathing session flow end-to-end, verify PWA installability, and confirm offline functionality.
- [x] 026: IndexedDB storage migration | Install `idb`, create IndexedDB schema with stores for `breathing_sessions`, `cold_sessions`, `preferences`, `program_progress`, `custom_presets`, and `milestones`, then swap `lib/storage.ts` internals from localStorage to IndexedDB while keeping the same exported API surface, and add a one-time migration that copies existing localStorage data on first load.
- [x] 027: Extract shared UI utilities | Deduplicate `OptionButton`, `formatTime`, and `formatDuration` patterns currently repeated across `app/breathe/page.tsx`, `app/cold/page.tsx`, `app/settings/page.tsx`, and `app/progress/page.tsx` into `components/ui/OptionButton.tsx` and `lib/format.ts`, then update all consumers.
- [x] 028: Externalize user-facing strings | Create `lib/i18n.ts` with a structured constants object containing all user-facing strings (organized by screen/feature), then replace hardcoded strings across all existing components and pages to reference `lib/i18n.ts`.
- [x] 029: Split large page components | Break `app/breathe/page.tsx` config UI into `components/breathing/BreathingConfig.tsx`, extract progress tab contents from `app/progress/page.tsx` into `components/progress/BreathingHistory.tsx` and `components/progress/ColdHistory.tsx`, and extract cold timer logic from `app/cold/page.tsx` into `components/cold/ColdTimer.tsx`.
- [x] 030: Extend SessionConfig types | Add `breathsPerRound: number[]`, `mindsetPrompts?: string[]`, `retentionMode: "free" | "target"`, and `autoCold: boolean` to `SessionConfig` in `lib/storage.ts`, update defaults and ensure backward compatibility with existing saved sessions.
- [x] 031: Screen Wake Lock | Implement Wake Lock API integration in a `lib/wakeLock.ts` utility, activate it during active breathing sessions in `SessionRunner.tsx` and cold timer in `ColdTimer.tsx`, release on session end, and add an on/off toggle in `app/settings/page.tsx`.
- [x] 032: Onboarding welcome flow | Build `app/onboarding/page.tsx` with 5 swipeable screens (hero, three pillars, physical expectations, safety acknowledgment, starting point picker), skip button after screen 2 (except mandatory safety screen 4), and update `app/page.tsx` redirect logic to route to onboarding when `onboardingComplete` is false.
- [x] 033: Practice profile setup | Add a profile setup step after onboarding screen 5 (and accessible from settings) collecting primary goal, available time, experience level, and preferred session time, store in `UserPreferences`, and use selections to set default `SessionConfig` values.
- [x] 034: First session guided mode | Build overlay components that wrap `SessionRunner.tsx` for the user's first session: pre-breathing explanation, a mid-session pause after round 1 with a "How did that feel?" prompt, and enhanced debrief text on `SessionComplete.tsx` explaining retention times, tracked via a `firstSessionComplete` flag in preferences.
- [x] 035: Per-round breath count UI | Update the breathing config screen (`BreathingConfig.tsx`) to allow setting different breath counts per round (e.g., [30, 40, 40]) and modify `PowerBreaths.tsx` to read `breathsPerRound[currentRound]` instead of a single count.
- [x] 036: Mindset prompts and retention modes | Add configurable mindset prompt text input per round in breathing config, display prompts during `RetentionHold.tsx`, and implement "target" retention mode showing a visual PB approach indicator alongside the existing "free" mode.
- [x] 037: Auto-launch cold and custom presets | Add `autoCold` toggle to breathing config that transitions to the cold timer after the last recovery breath instead of showing `SessionComplete`, and build a "Save as Preset" button plus preset management UI (up to 5 named presets) on the config screen and in settings.
- [x] 038: Program data model | Create `lib/program.ts` with `Program`, `ProgramDay`, and `ProgramProgress` types, define the full 30-day beginner program content (Week 1: 2 rounds/30 breaths, Week 2: 3/30 + cold, Week 3: 3/40 + longer cold, Week 4: 4 rounds/silence), and add program progress CRUD functions to `lib/storage.ts`.
- [x] 039: Program screen | Build `app/(app)/program/page.tsx` with a calendar view of program progress, today's session card showing prescribed breathing config and cold target, "Start Today's Session" button launching `SessionRunner` with the day's config, and pause/resume controls.
- [x] 040: Navigation update for Program tab | Update `components/BottomNav.tsx` to add a "Program" tab (5-tab layout), add the program icon, and adjust spacing/styling for the new tab count.
- [x] 041: Method guide page | Create `lib/content.ts` with 8 chapters of educational content (600-1000 words each covering technique, cold, mindset, science, side effects, advanced practices), build `app/(app)/learn/page.tsx` with chapter list and reader view, and gate chapters 4-8 behind 7 days of practice history.
- [x] 042: Session preparation tips | Build `components/SessionTip.tsx` that shows contextual tips on the breathing config screen based on time of day and recent session data (morning/evening/post-cold messages), and integrate it into `BreathingConfig.tsx`.
- [x] 043: Combined dashboard overview | Add an "Overview" tab (shown first) to `app/(app)/progress/page.tsx` with weekly session count comparison, average retention trend arrow, cold exposure total, current breathing + cold streaks, and consistency score.
- [x] 044: Weekly insights and enhanced charts | After 2+ weeks of data, show a smart insight card on the progress overview analyzing retention patterns and suggesting progression, and add per-round breakdown toggle, weekly average overlay, and trend line to `RetentionChart`.
- [x] 045: Daily reminder system | Implement Notification API permission request and daily reminder scheduling in `lib/notifications.ts`, skip if user practiced within 2 hours, and add on/off toggle with time picker to `app/settings/page.tsx`.
- [x] 046: Milestone notifications | Build `lib/milestones.ts` to detect achievements (first 2-min retention, first 3-min cold, 7/30/100-day streaks, new PB, program completions), store unlocked milestones in IndexedDB, and show in-app toast/modal celebrations via a `components/MilestoneToast.tsx`.
- [x] 047: Weekly summary card | Build `components/WeeklySummary.tsx` shown on the first app open after Monday with sessions vs previous week, average retention trend, cold total, streak status, and one actionable suggestion, dismissible until next week.
- [x] 048: Shareable session cards | Add a "Share" button to `SessionComplete.tsx` and the progress page that generates a shareable image (9:16 and 1:1 formats) using Canvas API with session stats, retention bar chart, streak badge, and app branding.
- [x] 049: Accessibility pass | Add ARIA labels to all interactive elements (breathing circle, timer, option buttons), announce phase transitions via `aria-live` regions in `SessionRunner.tsx`, add text alternatives for all charts, and implement vestibular-safe mode respecting `prefers-reduced-motion` with a settings toggle to replace `BreathingCircle` animation with a simple progress bar.
- [x] 050: Haptic mode and settings enhancements | Add "haptic" as a third audio mode option with distinct vibration patterns for inhale/exhale/hold via `navigator.vibrate()` in `lib/audio.ts`, add Celsius/Fahrenheit toggle, data export as JSON button, and practice profile link to `app/settings/page.tsx`.
- [x] 051: Fix SafetyOnboarding bypass | In `components/SafetyOnboarding.tsx`, stop setting `onboardingComplete: true` on acknowledge — only set `safetyAcknowledged: true` — and add a redirect guard in `app/(app)/layout.tsx` or `components/AppShell.tsx` that sends users with `onboardingComplete: false` to `/onboarding`.
- [x] 052: Fix retention averaging division by zero | Extract a `safeAvgRetention(retentionTimes: number[]): number` utility into `lib/analytics.ts` that guards against empty arrays, then replace raw reduce/length divisions in `components/progress/Overview.tsx`, `components/WeeklySummary.tsx`, and `components/progress/InsightCard.tsx`.
- [x] 053: Fix new_pb milestone re-triggering | Update `lib/milestones.ts` `unlockMilestone()` to allow repeatable milestone types like `new_pb` by storing each occurrence with a unique ID, so every new personal best triggers the toast with the new and previous time.
- [x] 054: Fix MilestoneToast subscription leak | In `components/MilestoneToast.tsx`, remove `current` from the `useEffect` dependency array and use a ref to track display state, preventing the subscription from being torn down and recreated on each milestone show/dismiss.
- [x] 055: Fix WeeklySummary localStorage error | Wrap all `localStorage.getItem`/`setItem` calls in `components/WeeklySummary.tsx` in try-catch blocks so the component doesn't crash in iOS private browsing or restricted storage environments.
- [x] 056: Add loading state to StorageProvider and root | Replace the `return null` in `components/StorageProvider.tsx` and `app/page.tsx` with a branded, dark-mode-aware loading spinner shown while IndexedDB initializes, eliminating the blank white flash.
- [x] 057: Fix onboarding safe area padding | Replace non-existent `pt-safe-top`/`pb-safe-bottom` classes in `app/onboarding/page.tsx` with working `env(safe-area-inset-*)` values via Tailwind arbitrary values or inline styles so content isn't obscured by the iOS notch/home bar.
- [x] 058: Add reduced-motion CSS overrides | Add a `@media (prefers-reduced-motion: reduce)` block in `app/globals.css` that disables `page-enter` and `slide-up` keyframe animations so page transitions and milestone toasts appear instantly for motion-sensitive users.
- [x] 059: Consolidate streak calculation | Create a single `calculateStreak(sessions: {date: string}[]): number` function in `lib/analytics.ts` and replace all 5 independent implementations in `lib/milestones.ts`, `lib/shareCard.ts`, `components/WeeklySummary.tsx`, `components/progress/Overview.tsx`, and `components/progress/ColdHistory.tsx`.
- [x] 060: Extract shared analytics utilities | Move duplicated `formatSeconds()`, `getMondayOfWeek()`/`startOfWeek()`/`getWeekStart()`, `avgRetention()`, and `getWeeklyComparison()` logic from component files into `lib/analytics.ts`, then update all consumers to import from the single source.
- [x] 061: Remove dead code | Delete `lib/constants.ts` entirely, remove unused exports `fromStorageCelsius` from `lib/format.ts`, `deleteBreathingSession`/`deleteColdSession` from `lib/storage.ts`, and `getProgramById`/`getAllPrograms` from `lib/program.ts`.
- [x] 062: Fix TypeScript types in DB schema | In `lib/db.ts`, type the `preferences` store value as `UserPreferences` instead of `unknown`, type `milestones.data` properly, and fix the `breathsPerRound` schema drift between DB schema (`number`) and `SessionConfig` (`number[]`).
- [x] 063: Add IndexedDB data validation | In `lib/storage.ts` `loadCache()`, validate that data loaded from IndexedDB matches expected shapes — skip invalid session records, fill missing preference fields with `DEFAULT_PREFERENCES` defaults, and wrap `JSON.parse` in `lib/notifications.ts` with schema checks.
- [x] 064: Add settings strings to i18n | Move hardcoded "Reduced Motion" title/description and any other raw English strings in `app/(app)/settings/page.tsx` into the `strings` object in `lib/i18n.ts`.
- [x] 065: Split oversized component files | Extract `PresetGrid`, `PerRoundConfig`, and `MindsetPrompts` from `BreathingConfig.tsx`; extract `AudioSettings`, `BreathingDefaults`, and `DataManagement` from `settings/page.tsx`; extract `RetentionChart` from `BreathingHistory.tsx`; and extract `ColdSessionLog` from `ColdTimer.tsx` — no file over 150 lines.
- [x] 066: Split oversized lib and page files | Extract program progress functions from `lib/storage.ts` into `lib/programStorage.ts`, split `lib/i18n.ts` into domain-specific string files, and extract `TodayCard`/`CalendarView` from `app/(app)/program/page.tsx` into separate component files.
- [x] 067: Add React error boundary | Create an error boundary component wrapping children in `app/(app)/layout.tsx` with a friendly error screen and "Try again" button that reloads the route, preventing unhandled render errors from crashing the entire app.
- [x] 068: Replace framer-motion with CSS animation | Rewrite the breathing circle animation in `components/breathing/BreathingCircle.tsx` using CSS keyframes and transitions, then remove `framer-motion` from `package.json`, reducing JS bundle by ~100KB gzipped.
- [x] 069: Replace next-pwa with serwist | Migrate from deprecated `next-pwa@5.6.0` to `serwist` (or `@ducanh2912/next-pwa`) in `next.config.mjs`, verify service worker generation, offline fallback page, and precaching of all app routes.
- [x] 070: Upgrade Next.js to 15.x | Upgrade `next` package from 14.2.5 to latest 15.x, fix any breaking changes in app router, middleware, or config, and verify `npm run build` and `npx tsc --noEmit` both pass.
- [x] 071: Lazy-load recharts on progress tab | Dynamic-import recharts in `components/progress/BreathingHistory.tsx` so the ~200KB bundle only loads when the user navigates to the Progress tab, with a loading skeleton placeholder.
- [x] 072: Remove userScalable false | Remove `userScalable: false` and `maximumScale: 1` from the viewport config in `app/layout.tsx` to fix WCAG 1.4.4 pinch-to-zoom accessibility violation.
- [x] 073: Fix manifest.json completeness | Add `id`, `categories: ["health", "fitness"]`, and a maskable 512px icon variant to `public/manifest.json` so the Lighthouse PWA audit passes.
- [x] 074: Add ARIA labels to cold timer ratings | Add `aria-label="{value} - {label}"` to the feeling rating buttons in `components/cold/ColdTimer.tsx` to match the accessible pattern already used in `SessionComplete.tsx`.
- [x] 075: Add privacy policy page | Create `app/(app)/privacy/page.tsx` with a static privacy policy stating all data is stored locally, what data is collected, and how to delete it, then link to it from `app/(app)/settings/page.tsx`.
- [x] 076: Add basic error tracking | Add minimal error tracking using `window.onerror` and `navigator.sendBeacon` in `app/layout.tsx` to capture unhandled promise rejections, error boundary catches, and IndexedDB failures — no user behavior or personal data tracked.
- [x] 077: Add semantic color tokens | Extend `tailwind.config.ts` with `cold`, `success`, `warning`, `danger`, `on-surface`, and `on-surface-light` color families per the design system spec, keeping existing `brand`, `accent`, and `surface` tokens intact.
- [x] 078: Migrate breathing components to color tokens | Replace all raw Tailwind color classes (`sky-*`, `gray-*`, `amber-*`) in `components/breathing/BreathingCircle.tsx`, `RetentionHold.tsx`, `RecoveryBreath.tsx`, `PowerBreaths.tsx`, `SessionComplete.tsx`, and `SessionRunner.tsx` with semantic token classes from `tailwind.config.ts`.
- [x] 079: Migrate cold and progress components to color tokens | Replace all raw Tailwind color classes in `components/cold/ColdTimer.tsx`, `ColdSessionLog.tsx`, `components/progress/Overview.tsx`, `ColdHistory.tsx`, `BreathingHistory.tsx`, `RetentionChart.tsx`, and `InsightCard.tsx` with semantic token classes.
- [x] 080: Migrate UI primitives and nav to color tokens | Replace raw color classes in `components/ui/Button.tsx`, `OptionButton.tsx`, `Card.tsx`, `Modal.tsx`, `components/BottomNav.tsx`, `components/MilestoneToast.tsx`, `components/WeeklySummary.tsx`, and `app/globals.css` body styles with semantic token classes.
- [x] 081: Migrate remaining files to color tokens | Replace raw color classes in `components/settings/*.tsx`, `components/breathing/PresetGrid.tsx`, `SavePresetForm.tsx`, `MindsetPrompts.tsx`, `PerRoundConfig.tsx`, `GuidedOverlay.tsx`, `SafetyOnboarding.tsx`, `SafetyReminder.tsx`, `ShareButton.tsx`, `ProfileSetup.tsx`, `LoadingScreen.tsx`, `ErrorBoundary.tsx`, and all page files — zero raw color classes should remain.
- [x] 082: Add animation timing tokens | Define `fast` (150ms), `normal` (250ms), and `slow` (400ms) as `transitionDuration` and `animation` extensions in `tailwind.config.ts`, then replace all hardcoded `duration-*` classes and inline `ms` values in `app/globals.css` and component files with the new tokens.
- [x] 083: Standardize page spacing | Remove `pb-20` from `components/AppShell.tsx`, add a unified `page-bottom` padding (`pb-[calc(5rem+env(safe-area-inset-bottom))]`) to each page container in `app/(app)/breathe/page.tsx`, `cold/page.tsx`, `progress/page.tsx`, `settings/page.tsx`, `program/page.tsx`, `learn/page.tsx`, and `privacy/page.tsx` so every page owns its own scroll padding.
- [x] 084: Standardize typography scale | Audit all text styling across components and align page titles to `text-2xl font-bold`, section headers to `text-sm font-semibold uppercase tracking-wider`, card titles to `text-lg font-semibold`, and timer displays to `tabular-nums` patterns per the spec's type scale in `SessionComplete.tsx`, `ColdTimer.tsx`, `Overview.tsx`, `WeeklySummary.tsx`, and all page headings.
- [x] 085: Increase touch targets to 44x44px | Increase padding on `OptionButton` (sm) in `components/ui/OptionButton.tsx`, preset delete buttons in `components/settings/DataManagement.tsx`, cold type selector buttons in `components/cold/ColdSessionLog.tsx`, and `Button` (sm) in `components/ui/Button.tsx` so all interactive elements meet the 44x44px WCAG minimum.
- [x] 086: Responsive breathing circle | Replace hardcoded `width: 200, height: 200` in `components/breathing/BreathingCircle.tsx` with viewport-relative sizing using `min(200px, 45vw)`, ensure the animation scale stays within container bounds, and make the reduced-motion progress bar (`w-48`) also viewport-relative.
- [x] 087: Responsive cold timer circle | Make `CircularProgress` radius in `components/cold/ColdTimer.tsx` viewport-relative instead of hardcoded `radius=110`, using an approach like `min(110, 22vw)` via a container or `useEffect` measurement, and scale the timer text inside proportionally.
- [x] 088: Retention hold long-press protection | Replace the single `onClick` on the "End breath hold" button in `components/breathing/RetentionHold.tsx` with a long-press gesture (500ms hold to end) with a circular fill animation, plus a double-tap fallback (two taps within 400ms), keeping single-tap accessible for screen readers via `aria-description`.
- [x] 089: Phase transition crossfade | Add a 250ms crossfade animation between phase transitions in `components/breathing/SessionRunner.tsx` using a CSS keyframe (e.g., `animate-phase-fade` in `app/globals.css`), fire `aria-live` announcements after transition completes, and skip the animation when reduced motion is preferred.
- [x] 090: Modal and overlay scroll safety | Add `max-h-[calc(100dvh-2rem)]` and `overflow-y-auto` to `components/ui/Modal.tsx` content, and apply the same constraint to `components/breathing/GuidedOverlay.tsx`, `components/WeeklySummary.tsx`, and `components/MilestoneToast.tsx` so all overlays are scrollable when content exceeds viewport height.
- [x] 091: Settings preset delete icon button | Replace the text "Delete" button for custom presets in `components/settings/DataManagement.tsx` (or wherever preset management renders) with a 44x44px trash icon button with `aria-label="Delete {preset name}"` that works on 320px viewports without layout overflow.
- [x] 092: Dark mode user toggle | Switch `darkMode` from `"media"` to `"class"` in `tailwind.config.ts`, add a System/Light/Dark toggle in `app/(app)/settings/page.tsx`, store preference in IndexedDB via `lib/storage.ts`, apply `dark` class to `<html>` in `app/layout.tsx` with an inline script to prevent flash of wrong theme on load.
- [x] 093: Icon consistency pass | Replace emoji icons in `components/MilestoneToast.tsx` (`ICON_MAP`) with inline SVG components, create a shared `components/ui/Icon.tsx` wrapper with size props (sm/md/lg), and ensure all icons use `currentColor` for theme-aware coloring across `BottomNav.tsx` and milestone toasts.
- [x] 094: BottomNav height CSS custom property | Set a `--nav-height` CSS custom property on the `<nav>` element in `components/BottomNav.tsx` that includes `env(safe-area-inset-bottom)`, then update the `page-bottom` padding in all page containers to reference `var(--nav-height)` as a single source of truth.
- [ ] 095: Reduced-motion audit for all animations | Ensure every animation in the app (breathing circle `breathe` keyframe, cold timer `CircularProgress` transition, `animate-slide-up`, `animate-page-enter`, and any inline transition styles) respects both `prefers-reduced-motion` and the app's reduced motion toggle in `lib/useReducedMotion.ts`.
- [ ] 096: Add ESLint to CI pipeline | Add `npm run lint` step to `.github/workflows/ci.yml` after type-check and before build, enable `eslint-plugin-react-hooks` exhaustive-deps rule and set `no-unused-vars` to error in `.eslintrc.json`.
- [ ] 097: Add Prettier for code formatting | Install Prettier, create `.prettierrc` config, add `format` and `format:check` scripts to `package.json`, run `npx prettier --write .` for a one-time format pass, and add `format:check` to `.github/workflows/ci.yml`.
- [ ] 098: Configure Vitest for unit testing | Install Vitest with TypeScript support, add `test` and `test:coverage` scripts to `package.json`, create `vitest.config.ts` with path aliases matching `tsconfig.json`, add `npm test` step to `.github/workflows/ci.yml`, and write a smoke test for `lib/analytics.ts` `calculateStreak`.
- [ ] 099: Add unit tests for analytics and milestones | Write Vitest tests for all functions in `lib/analytics.ts` (streak calculation, safe averaging, week boundaries, weekly comparison) and `lib/milestones.ts` (milestone detection, repeatable milestones, edge cases) targeting 80%+ coverage on both files.
- [ ] 100: Add unit tests for storage validation | Write Vitest tests for data validation logic in `lib/storage.ts` (invalid session records, missing preference fields, migration edge cases) and `lib/format.ts` (time formatting, temperature conversion) targeting 80%+ coverage.
- [ ] 101: Add pre-commit hooks | Install Husky and lint-staged, configure pre-commit hook to run Prettier and ESLint on staged files via lint-staged plus `tsc --noEmit` for type checking, ensuring all commits pass quality checks.
